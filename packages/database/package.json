{
  "name": "@valley/db",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "db:generate": "pnpm db-env prisma generate",
    "db:push": "pnpm db-env prisma db push --skip-generate",
    "db:migrate": "pnpm db-env prisma migrate dev",
    "db:migrate:reset": "pnpm db-env prisma migrate reset",
    "db:studio": "pnpm db-env prisma studio",
    "db:seed": "pnpm db-env ts-node prisma/seed.ts",
    "db-env": "dotenvx run -f ../../.env --",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.14.2",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "eslint": "^8.56.11",
    "prisma": "^5.22.0"
  }
}
