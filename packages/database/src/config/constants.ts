export enum ProjectHeadingFont {
  CORMORANT_GARAMOND = 'Cormorant Garamond',
  FORUM = 'Forum',
  GEIST = 'Geist',
  JOST = 'Jost',
  MUSEO_SANS_CYRL = 'Museo Sans Cyrl',
  ROBOTO_CONDENSED = 'Roboto Condensed',
  SPECTRAL = 'Spectral',
}

export enum ProjectCoverVariant {
  CENTER = 0,
  SPLIT,
  LEFT,
  ALBUM,
  CLASSIC,
  LINE,
  TOP,
  INVERT,
}

export enum ProjectGalleryOrientation {
  VERTICAL = 0,
  HORIZONTAL = 1,
}

export enum ProjectGallerySpacing {
  SMALL = 0,
  MEDIUM,
  LARGE,
}

// TODO: keep in sync with @valley/shared#Theme
export enum ProjectGalleryTheme {
  DARK = 'dark',
  LIGHT = 'light',
  SYSTEM = 'system',
}
