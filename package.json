{
  "name": "valley",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "jarvis394",
  "license": "MIT",
  "scripts": {
    "dev": "turbo dev --filter !storybook",
    "docker:dev": "docker compose -f docker-compose.dev.yml up",
    "build": "NODE_ENV=production turbo build",
    "prisma:migrate": "yarn prisma migrate dev",
    "prepare": "husky",
    "lint": "turbo typecheck && turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@valley/eslint-config": "*",
    "@valley/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "^2.0.11"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "husky": "^9.1.4"
  }
}
